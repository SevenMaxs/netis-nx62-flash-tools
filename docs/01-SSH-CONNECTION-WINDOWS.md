
---

# 🔐 Настройка SSH доступа к Netis NX62 — Windows

> Руководство для Windows 10 / 11 с использованием встроенного OpenSSH.
> После выполнения этих шагов команды `ssh netis-nx62` и `scp -O … netis-nx62:…`
> будут работать из любого терминала (PowerShell, Windows Terminal, cmd).
>
> Инструкции [02-BACKUP-MTD](02-BACKUP-MTD.md) и [03-BACKUP-CONFIG](03-BACKUP-CONFIG.md)
> не потребуют никаких дополнительных изменений.

---

## Что нам понадобится

| Компонент | Откуда | Проверка |
|---|---|---|
| OpenSSH Client | Предустановлен в Windows 10 1809+ | `ssh -V` |
| PowerShell 5+ | Предустановлен | `$PSVersionTable.PSVersion` |
| Сеть до роутера | LAN / Wi-Fi | `ping 192.168.1.1` |

> [!TIP]
> Если `ssh -V` не распознаётся, включите компонент:
> **Параметры → Приложения → Дополнительные компоненты → Клиент OpenSSH**.

---

## Шаг 1. Убедитесь, что OpenSSH работает

Откройте **PowerShell** и выполните:

```powershell
ssh -V
```

Ожидаемый ответ — строка вида `OpenSSH_for_Windows_9.x`.

---

## Шаг 2. Сгенерируйте SSH-ключ

```powershell
# Создаём папку для ключей (если ещё нет)
New-Item -ItemType Directory -Force "$HOME/.ssh" | Out-Null

# Удаляем старую пару ключей, если она есть (иначе ssh-keygen спросит «Overwrite?»)
Remove-Item "$HOME/.ssh/dropbear_key", "$HOME/.ssh/dropbear_key.pub" -ErrorAction SilentlyContinue

# Генерируем RSA-2048 ключ без пароля
ssh-keygen -t rsa -b 2048 -N '""' -f "$HOME/.ssh/dropbear_key"
```

После выполнения в `$HOME/.ssh/` появятся два файла:

| Файл | Назначение |
|---|---|
| `dropbear_key` | Приватный ключ (не передавайте никому!) |
| `dropbear_key.pub` | Публичный ключ (отправляется на роутер) |

---

## Шаг 3. Загрузите публичный ключ на роутер

При первом подключении введите пароль от роутера (по умолчанию — пароль учётной записи `root`).

```powershell
Get-Content "$HOME/.ssh/dropbear_key.pub" |
    ssh root@192.168.1.1 'cat >> /etc/dropbear/authorized_keys'
```

> [!NOTE]
> Dropbear (SSH-сервер на роутере) хранит авторизованные ключи
> в `/etc/dropbear/authorized_keys`, а не в `~/.ssh/authorized_keys`.

---

## Шаг 4. Создайте SSH-алиас `netis-nx62`

Именно этот алиас используют последующие инструкции по бекапу.

```powershell
$sshConfig = "$HOME/.ssh/config"

$entry = @"

# Netis NX62 Router
Host netis-nx62
    HostName 192.168.1.1
    User root
    IdentityFile ~/.ssh/dropbear_key
    IdentitiesOnly yes
"@

# Добавляем блок в конец конфига (файл создастся автоматически)
Add-Content -Path $sshConfig -Value $entry -Encoding UTF8
```

<details>
<summary>📄 Итоговый фрагмент <code>~/.ssh/config</code></summary>

```
# Netis NX62 Router
Host netis-nx62
    HostName 192.168.1.1
    User root
    IdentityFile ~/.ssh/dropbear_key
    IdentitiesOnly yes
```
</details>

---

## Шаг 5. Проверьте подключение

```powershell
ssh netis-nx62 'echo "SSH работает! Хост: $(cat /proc/sys/kernel/hostname)"'
```

Если всё настроено верно, вы увидите имя хоста роутера **без запроса пароля**.

---

## 🔧 Устранение неполадок

| Проблема | Решение |
|---|---|
| `Connection refused` | Убедитесь, что SSH-сервер включён на роутере (порт 22) |
| `Permission denied (publickey)` | Проверьте, что ключ загружен: `ssh root@192.168.1.1` с паролем → `cat /etc/dropbear/authorized_keys` |
| `WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED` | Удалите старую запись: `ssh-keygen -R 192.168.1.1` |
| `ssh` не найден | Установите компонент **Клиент OpenSSH** (см. начало инструкции) |

---

### 📌 Что дальше

- Подключайтесь к роутеру одной командой: `ssh netis-nx62`
- Копируйте файлы на роутер: `scp -O файл netis-nx62:/tmp/`
- **Сделайте бекап MTD** → [02-BACKUP-MTD.md](02-BACKUP-MTD.md)
- **Сделайте бекап конфигов** → [03-BACKUP-CONFIG.md](03-BACKUP-CONFIG.md)

---

<div align="center">
<b>Netis NX62 Flash Tools</b> | <a href="https://github.com/SevenMaxs/netis-nx62-flash-tools">GitHub</a>
</div>
